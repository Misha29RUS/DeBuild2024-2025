spring:
    application:
      name: billing-core

    datasource:
      url: jdbc:postgresql://localhost:5435/billing-system-db
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver

    jpa:
      show-sql: true

server:
  port: 8080